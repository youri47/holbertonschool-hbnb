"""Persistence layer for the application.

This package provides abstract and concrete implementations of the Repository pattern
for data access and storage. It serves as an abstraction layer between the business
logic and the underlying data storage mechanism.

Modules:
    - repository: Defines the abstract Repository interface
    - in_memory_repository: In-memory implementation of the Repository interface
"""

"""Persistence layer for the application.

This package provides abstract and concrete implementations of the Repository pattern
for data access and storage. It serves as an abstraction layer between the business
logic and the underlying data storage mechanism.

Modules:
    - repository: Defines the abstract Repository interface
    - in_memory_repository: In-memory implementation of the Repository interface
"""